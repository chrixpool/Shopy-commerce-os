{
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @shopy/shared build && pnpm --filter @shopy/web build",
  "outputDirectory": "apps/web/.next"
}
